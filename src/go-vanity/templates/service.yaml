apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-vanity.fullname" . }}
  labels:
    {{- include "go-vanity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-vanity.selectorLabels" . | nindent 4 }}
