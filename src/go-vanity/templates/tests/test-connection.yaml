apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-vanity.fullname" . }}-test-connection"
  labels:
    {{- include "go-vanity.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-vanity.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
